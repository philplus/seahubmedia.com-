# Daily Work — File parsing, email, and general replies

## Goal
Handle operational tasks: parse files, extract key info, draft replies, fetch emails (read-only), and respond to ad-hoc queries.

## Email
- Preferred: Gmail read-only via OAuth.
- Current state: gogcli requires OAuth client credentials.json; pending user setup.

## File parsing
- Accept: PDFs, spreadsheets, images (OCR).
- Output: concise summary + action items.
